\section{Evaluation}

We evaluate our method by comparing the reconstructed XYZ color matching functions from our selected filters to the target XYZ functions, summarized by the MSE loss. 
We also consider the color difference $\Delta E_{2000}$ for a set of reflectances.
This latter method effectively approximates the human eye's response to color difference, and is therefore well suited for a practical evaluation of our reconstruction \cite{farnand2003using}. 
For the latter evaluation, we use the Macbeth Color Checker \cite{Pascale2006RGBCO} as our test set, and render XYZs and our RGBs  (found from our method to best approximate XYZs) under a D65 illuminant. 

In Figure 6, we show the best $\Delta E_{2000}$ of our approximation at each $k$ filter subset of the Rosco Supergel set, where $k\in [1,8]$. For completeness, we use brute force search. 
%For each number of filters $N$, we compute the minimum $\Delta E_{2000}$ across all combinations of $N$ filters from the Rosco Supergel set, and plot the relationship between $N$ and $\Delta E_{2000}$.

\begin{center}
    \label{fig:DeltaE}
    \centering
    \includegraphics[width=\linewidth]{media/deltaE_vs_k_mse.png}
    \captionof{figure}{Best $\Delta E_{2000}$ for each number of filters $k$ }
\end{center}

We also plot the resultant reconstructed XYZ functions for the best $k-$filter combination of $k\in\{3,4,5,6\}$, see respectively Figures 7, 9, 10 and 12. 
%In the $k=3$ case, we can see visible artifacts in the transmission functions. 

%%% GRAHAM GOT TO HERE 19/4/26

\begin{center}
    \centering
    \includegraphics[width=\linewidth]{media/k3top1_spectra.png}
    \captionof{figure}{Camera Spectral Sensitivity for best XYZ Reconstruction for $k=3$ }
\end{center}

The best filter combination for $k=3$ - clearly is a poor spectral fit, see Fig. 7 -  achieves a tolerable mean $\Delta E_{2000}$ of 3.77, which is above the just noticeable difference threshold of 2.3,
%% KYLE ... not sure where the 2.3 comes from. Put a reference in here.
indicating that the reconstructed colors may be distinguishable from the target colors under certain conditions.
The 24 patches below show the original in the top left of each pane and the simulated reconstruction in the bottom left. It is evident there is a visible colour difference.

\begin{center}
    \centering
    \includegraphics[width=\linewidth]{media/n3MSEMacbeth.png}
    \captionof{figure}{Macbeth Color Checker Comparison for $k=3$}
\end{center}
%\begin{center}
%    \centering
%    \includegraphics[width=\linewidth]{media/n3MSEbarRank2.png}
%    \captionof{figure}{Best $\Delta E_{2000}$ for each number of filters $N=3$ }
%\end{center}

We see a noticeable improvement in MSE and in $\Delta E_{2000}$ with $k=4$, the latter just above the {\color{red} `}just noticeable difference' of 1.0. 
\begin{center}
    \centering
    \includegraphics[width=\linewidth]{media/k4top1_spectra.png}
    \captionof{figure}{Camera Spectral Sensitivity for best XYZ Reconstruction for $k=4$}
\end{center}

Next, $k=5$ achieves a $\Delta E_{2000}$ of 0.35, which is well below the just noticeable difference threshold, indicating that the reconstructed colors are likely indistinguishable from the target colors under typical viewing conditions.

\begin{center}
    \centering
    \includegraphics[width=\linewidth]{media/k5top1_spectra.png}
    \captionof{figure}{Camera Spectral Sensitivity for best XYZ Reconstruction for $k=5$ }
\end{center}
The following Macbeth color patches display nearly identical hues between target and simulated patches. 
\begin{center}
    \centering
    \includegraphics[width=\linewidth]{media/k5Macbeth_render_rank_0.png}
    \captionof{figure}{Macbeth Color Checker Comparison for $k=5$ }
\end{center}

%The distribution of $\Delta E_{2000}$ across the 24 Macbeth Color Checker patches for the best filter combination for $k=10$ shows that all patches achieve a $\Delta E_{2000}$ below 0.1.

%\begin{center}
%    \centering
%    \includegraphics[width=\linewidth]{media/n10MSEbarRank0.png}
%    \captionof{figure}{Best $\Delta E_{2000}$ for each number of filters $N$ }
%\end{center}

%In addition, the $3 \times 10$ $\boldsymbol{M}$ matrix for the best $k=10$ reconstruction is shown below.
%We note that all 10 filters contribute to at least two of the trichromatic values.
%\begin{center}
%    \centering
%    \includegraphics[width=\linewidth]{media/n10M.png}
%    \captionof{figure}{$\boldsymbol{M}$ matrix for $k=10$ reconstruction}
%\end{center}

Finally, $k=6$ achieves a $\Delta E_{2000}$ of 0.39, and there is generally limited further decrease in $\Delta E_{2000}$ at $k>6$. Notice here the spectral fit the XYZ CMFs is good.

\begin{center}
    \centering
    \includegraphics[width=\linewidth]{media/k6top1_spectra.png}
    \captionof{figure}{Camera Spectral Sensitivity for best XYZ Reconstruction for $k=6$ }
\end{center}
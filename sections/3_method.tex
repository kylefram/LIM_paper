\section{Method}
\subsection{Data Collection}
We take the standard CIE 1931 XYZ color matching functions \cite{1931TrOS...33...73S} as our target spectral response functions for reconstruction.

First, we draft a sample set of filters by scraping the  

For our measured filter set, we use a standard set of Rosco Supergel \cite{rosco_mycolor} filters and measure their spectral response functions using a spectrophotometer under an Ianiro Varibeam 800W bulb.
We construct a dataset spanning $\lambda$ 400-750 at 1nm intervals, and normalize the response functions to a maximum of 1.

%SUPERGEL PHOTO
\begin{center}[H]
    \centering
    \includegraphics[width=\linewidth]{media/measure2.jpeg}
    \captionof{figure}{Filter measurement setup with spectrophotometer and Rosco Supergel filters.}
\end{center}

\subsection{Formalization}

We minimize for the distance between the target spectral response functions and the reconstructed functions from the selected filters:
\begin{equation}
    \argmin_{\boldsymbol{M},f} \int_{\lambda}  \boldsymbol{L}(\boldsymbol{P}(\lambda),\boldsymbol{G}_{\boldsymbol{M}}(\lambda))d\lambda
    \label{eq:1}
\end{equation}

We consider all color channels equally and decompose target color matching functions $\boldsymbol{P}(\lambda)$ and simulated color matching functions $\boldsymbol{G}(\lambda)$ into $p_c(\lambda)$ and $G_c(\lambda)$, respectively.

\begin{equation}
    \argmin_{\boldsymbol{M},f} \sum_{c \in {X,Y,Z}} \int_{\lambda}  \boldsymbol{L}(p_c(\lambda),G_{c,\boldsymbol{M}}(\lambda))d\lambda
    \label{eq:2}
\end{equation}


Where $G_{c,\boldsymbol{M}}(\lambda)$ is the reconstructed spectral response function for color channel $c$ from the selected filters, which can be expressed as:
\begin{equation}
    G_{c,\boldsymbol{M}}(\lambda) = \sum_{k}^{N} f_k M_{c}I(\lambda)R(\lambda)e_{k}(\lambda) \, d\lambda
    \label{eq:3}
\end{equation}

\begin{center}
\captionof{table}{Notation and symbols}
\begin{tabular}{ll}
\toprule
Symbol & Description \\
\midrule
$N$ & number of filters \\
$\boldsymbol{M}$ & $3 \times N$ matrix of weights\\
$\boldsymbol{L}$ & Loss function \\
$\boldsymbol{P}(\lambda)$ & Target color matching function\\
%$p_c(\lambda)$ & Target color matching function, target $\in {X,Y,Z}$\\
$\boldsymbol{G_M}(\lambda)$ & Simulated color matching function as per \ref{eq:2}\\
%$p_c(\lambda)$ & Target color matching function, target $\in {X,Y,Z}$\\

%$M_c$ & row of $\boldsymbol{M}$ corresponding to $c \in \{R,G,B\}$\\
$f_k$ & $N \times 1$ filter selection vector, items $\in {0,1}$ \\
$I(\lambda)$ & Illuminant intensity \\
$R(\lambda)$ & Surface reflectance spectrum \\
$e_k(\lambda)$ & Filter $k$ Spectral Sensitivity function\\
%$q(\lambda)$ & Camera spectral sensitivity \\


%$p_d(\lambda)$ & CIE XYZ color matching functions, $d \in \{X,Y,Z\}$ \\
%$\boldsymbol{M}$ & $3 \times 3$ color-correction matrix with elements $m_{c,d}$ \\$\boldsymbol{v}$ & a row vector with RAW linear RGB color values \\
%$\boldsymbol{u}$ & a row vector with CIE 1931 XYZ tristimulus values \\
\bottomrule
\end{tabular}
\end{center}

For approximations of XYZ, we follow the work of Hardeberg \cite{Filterselection} in using RMSE as our loss function, again equally weighted across each channel. 
Additionally, with RMSE loss function, the solution to \ref{eq:1} can be achieved in a computationally efficient manner by computing the pseudoinverse.
\begin{equation}
    \argmin_{\boldsymbol{M}} ||\boldsymbol{P} - \boldsymbol{M}\boldsymbol{G_M}||^2
\end{equation}
For $F$ as Mean Squared Error (MSE), The optimal weights can be found using the pseudo-inverse:
\begin{equation}
    \boldsymbol{M} = \boldsymbol{P}\boldsymbol{G_M}^T(\boldsymbol{G_M}\boldsymbol{G_M}^T)^{-1}
\end{equation}  

And for other $F$, such as SMAPE, we use gradient descent to find the optimal weights, informing our initial guess with the MSE solution.

\subsection{Filter Selection}
\subsubsection{Brute Force Search}
In the naiive solution, we find the optimal filter combination for each $N$ by selecting the combination with the lowest loss $F$, considering all combinations of filters.
We plot the errors across all combinations for N=4:

\begin{center}
    \centering
    \includegraphics[width=\linewidth]{media/all_errorsN4MSE.png}
    \captionof{figure}{MSE Error distribution across all filter combinations for N=4}
\end{center}

As the search space grows combinatorially with $N$, we consider $N$ up to 10, which is a reasonable number of filters for a filter wheel system.

\subsubsection{LASSO Regression}
To find the optimal filter combination for each $N$ without brute force search, we can use LASSO regression to find the optimal weights $M$ for all filters, and then select the top $N$ filters with the highest weights.


\subsection{$M$ Optimization}
For each combination of filters $f_k$, we optimize for the weights $M_{c,k}$ to minimize the loss $F$ between the target spectral response functions and the reconstructed functions from the selected filters.
Following Hardeberg\cite{Filterselection} and Maitre it al.\cite{maitre1996spectrophotometric}, we utilize a PCA decomposition to maximize the orthogonality of the selected filters in relation to an arbitrary target. 
